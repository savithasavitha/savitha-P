#include<stdio.h>
int add numbers(int n);
int main()
{
int num;
printf("enter a positive integer:");
scanf("%d",&num);
printf("sum=%d",add numbers(num));
return 0;
}
if(n!=0)
return+add numbers(n-1);
else
return n;
}
