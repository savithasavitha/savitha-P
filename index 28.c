#incliude<stdio.h>
#define MAXSIZE 1000
int main()
{
int array[MAX SIZE];
int i,N;
printf("enter the size of array;");
scanf("%d",&N);
printf("enter %d elements in the array:",N);
for(i=0;i<N;i++)
{
scanf("%d",&array[i]);
}
printf("\n in array are:");
for(i=0;i<N;i++)
printf("%d',array[i]);
}
return 0;
}
